# Data

The source code expects the research project's `data/` directory at repository root.

Do not commit the Phase 7 raw hydroponic archive unless its original license explicitly permits redistribution. Preserve raw source files unchanged and document analytical corrections separately.

Synthetic datasets can be regenerated using the repository scripts where supported.
